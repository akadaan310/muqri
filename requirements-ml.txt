# Neural components: CTC forced alignment (wav2vec2) and ECAPA-TDNN timbre embeddings.
# CPU-only wheels: pip install --index-url https://download.pytorch.org/whl/cpu torch torchaudio
-r requirements.txt
torch>=2.1
torchaudio>=2.1
transformers>=4.40
speechbrain>=1.0

# Optional HTTP API (python main.py serve)
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9
